# task-17 — 지표 · CI · 배포

> 선행: task-16
> 설계 근거: [becontext.md](../../becontext.md) · [db-schema.md](db-schema.md)

## 목표

`events` 10종 로깅 + 지표 쿼리 + `backend-ci.yml` + 배포.

## 확정본 반영 (becontext.md 대비 변경)

- 필수 지표 3개 — 평균 `depth`(꼬리질문 깊이), `tool_name IS NULL` vs 값 있음 비율(근거 조회가 실제로 도는지), `selection_source=ai_removed` 목록(매칭 로직 수정 근거).

## 작업

- [ ] TODO

## 완료 조건

- [ ] TODO

## 커밋 메시지

```
TODO
```
