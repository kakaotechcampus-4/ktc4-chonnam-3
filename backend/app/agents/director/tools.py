"""Backend I/O adapter boundary for ``devon_ai.agents.director.tools``.

The canonical AI module owns tool reasoning. The backend retains authorization and external
evidence access, with no facade exposed until the tool contracts are implemented.
"""
